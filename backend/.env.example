# Flask Configuration
FLASK_DEBUG=False
SECRET_KEY=change-this-to-a-random-secret-key-in-production
HOST=0.0.0.0
PORT=5000

# CORS Configuration (comma-separated origins)
# For development: http://localhost:3000
# For production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:3000

# Model Configuration
MODEL_PATH=models/advanced_bp_model.keras
FALLBACK_MODEL_PATH=models/blood_pressure_model.keras

# Data Directories
DATA_DIRECTORY=data
MODELS_DIRECTORY=models
LOGS_DIRECTORY=logs

# API Configuration
MAX_CONTENT_LENGTH=16777216

# Logging
LOG_LEVEL=INFO

# Database (if needed in future)
# DATABASE_URL=postgresql://user:password@localhost:5432/bppredict

# Security (if implementing authentication)
# JWT_SECRET_KEY=change-this-jwt-secret-in-production
# JWT_ACCESS_TOKEN_EXPIRES=3600

# Redis (if implementing caching)
# REDIS_URL=redis://localhost:6379/0
